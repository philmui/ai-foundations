[project]
name = "module-04-vectorization-broadcasting"
version = "0.1.0"
description = "Module 4: Vectorization & Broadcasting"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.0.0",
    "numpy>=1.26.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
